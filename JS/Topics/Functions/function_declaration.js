//  Function Declaration (Named Function)

// A standard way to define a function using the function keyword. 
// These are hoisted, meaning they can be called before they appear in the code. 

function greet(name) {
  return `Hello, ${name}!`;
}


